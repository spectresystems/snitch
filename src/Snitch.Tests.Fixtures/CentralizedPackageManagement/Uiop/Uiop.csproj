<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <!-- Pins a version inline even though versions are managed centrally. NuGet rejects
       this during a normal restore, but the inline version is what must win here. -->
  <ItemGroup>
    <PackageReference Include="Autofac" Version="4.9.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Qwerty\Qwerty.csproj" />
  </ItemGroup>

</Project>
